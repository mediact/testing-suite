imports:
  - resource: 'config/default/grumphp.yml'

parameters:
  composer.strict: false
  xmllint.ignore_patterns:
    - /^phpcs.xml$/
    - /^phpmd.xml$/
    - /^phpunit.xml$/
    - /^pdepend.xml$/
    - /^templates/
